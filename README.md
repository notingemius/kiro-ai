# Kiro AI — Reply Helper

Chrome-расширение (Manifest V3). Выдели текст на любой странице — справа появится кнопка **K**. Клик → панель с подсказкой от AI:

- **Тема ответа** — одной фразой
- **План** — шаги, по которым строится ответ
- **Шаблон ответа** в четырёх частях:
  - **Реакция** на его слова
  - **Эмоция / наблюдение** от себя
  - **Кусочек себя** — личный штрих, опыт, мысль
  - **Вопрос, который копает глубже**

Копируешь шаблон, дописываешь своё, отправляешь. Отвечаешь сам — AI не заменяет тебя, а помогает сформулировать живо и по-человечески.

## Установка

1. Скачай репо: `git clone https://github.com/notingemius/kiro-ai.git` или [ZIP](https://github.com/notingemius/kiro-ai/archive/refs/heads/main.zip)
2. Открой в Chrome: `chrome://extensions`
3. Включи «Режим разработчика» (правый верхний угол)
4. «Загрузить распакованное расширение» → выбери папку `kiro-ai/`
5. Кликни на иконку расширения → откроется страница настроек
6. Вставь API-ключ OpenRouter (https://openrouter.ai/keys) и выбери модель
7. Сохрани

## Бенчмарк моделей

Не знаешь какая модель быстрее? Открой настройки расширения → кнопка **«Тест скорости моделей →»**.

Страница прогонит все модели с твоим API-ключом, замерит реальное время ответа (с учётом твоей географии и текущей нагрузки), покажет таблицу с сортировкой по скорости. Одним кликом можно поставить самую быструю как дефолт.

## Обновление

```
git pull
```
Потом в `chrome://extensions` нажми **⟳** на карточке Kiro AI.

## AI-провайдер

Используется [OpenRouter](https://openrouter.ai/) — там есть **бесплатные** модели (суффикс `:free`). Карта при регистрации не нужна. Лимиты free-моделей: ~20 запросов / мин, ~50 / день.

### Быстрые модели (рекомендую для живого чата, 3–6 сек)

- `deepseek/deepseek-chat-v3.1:free` — **дефолт**
- `z-ai/glm-4.5-air:free`
- `meta-llama/llama-3.3-70b-instruct:free`
- `google/gemma-3-27b-it:free`
- `qwen/qwen3-235b-a22b:free`

### Reasoning (умнее, но 15–25 сек)

- `openai/gpt-oss-120b:free`
- `openai/gpt-oss-20b:free`
- `nvidia/nemotron-nano-9b-v2:free`

> Reasoning автоматически отключается запросом `reasoning: { enabled: false }` — модели отдают ответ быстрее, без длинного chain-of-thought.

Можно вписать свой slug в настройках.

## Как пользоваться

- Выдели текст мышкой → кликни на появившуюся кнопку **K** (или ПКМ → «Kiro AI: подсказать ответ»)
- Панель покажет подсказку (с счётчиком секунд «Думаю… 3с»)
- «Скопировать шаблон» → вставь в чат, поправь под себя, отправь
- «Ещё вариант» → попросить другой вариант
- `Esc` — закрыть

## Структура

```
kiro-ai/
├── manifest.json          # MV3 манифест
├── icons/                 # 16/48/128 PNG + SVG исходник
├── scripts/gen_icons.py   # генератор иконок (stdlib, без Pillow)
└── src/
    ├── background.js      # service worker: вызов OpenRouter API
    ├── content.js         # обработка выделения + UI на странице
    ├── content.css        # стили триггера и панели
    ├── options.html       # страница настроек
    ├── options.js         # логика настроек
    └── options.css        # стили настроек
```

## Приватность

- Ключ хранится в `chrome.storage.sync` — не уходит никуда, кроме как в openrouter.ai.
- Выделенный текст отправляется только на OpenRouter.
- На бесплатных `:free` моделях провайдеры могут использовать запросы для обучения. Не вставляй туда чувствительную переписку.

## Лицензия

MIT
